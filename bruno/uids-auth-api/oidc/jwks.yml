info:
  name: JWKS
  type: http
  seq: 2

http:
  method: GET
  url: "{{authBaseUrl}}/.well-known/jwks.json"
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Public signing keys for JWT verification. Resource servers (`requireAuth`) use this URI.
