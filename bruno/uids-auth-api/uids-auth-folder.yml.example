# Copy to your existing Bruno collection as: uids-auth/folder.yml
# (after copying oidc/, oauth/, devices/, email/, session/, social/ under uids-auth/)

info:
  name: UIDs Auth Server
  type: folder
  seq: 50

docs: |-
  HTTP API from @advcomm/uids-io-auth (`createAuthRouter`).
  Base URL: {{authBaseUrl}}

  Merge env vars from bruno/uids-auth-api/environments/local.yml into your collection environments.
