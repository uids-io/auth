info:
  name: Get Session
  type: http
  seq: 1

http:
  method: GET
  url: "{{authBaseUrl}}/session"
  auth: inherit

settings:
  encodeUrl: true

docs: |-
  Returns session and user when `uids_auth_session` cookie is valid. Use browser cookie jar or manual cookie in Bruno.
